# Contact form — server-side only.
# Create an API key at https://resend.com and paste it here in your local .env.local (never commit).
RESEND_API_KEY=

# Address that receives contact form submissions.
CONTACT_TO_EMAIL=nathaliegp1411@gmail.com

# Verified sender address configured in Resend (e.g. onboarding@resend.dev or your custom domain sender).
CONTACT_FROM_EMAIL=onboarding@resend.dev
