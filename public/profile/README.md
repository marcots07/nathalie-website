# Profile photo

Drop the hero portrait here as `nathalie.jpg` (the New York bridge photo,
portrait orientation ~2:3). The hero crops it to 4:5 with the framing
centered slightly above the middle so the face stays in view. Until the
file exists, the clean placeholder block renders instead.
