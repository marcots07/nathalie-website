# Leaf — screenshots

Drop the app screenshots here. Currently referenced by
`content/projects/leaf/project.json`:

| File            | What it is                              | Used in                          |
| --------------- | --------------------------------------- | -------------------------------- |
| `home-lofi.png` | Home screen, low-fidelity wireframe     | Before/after slider (left side)  |
| `home-hifi.png` | Home screen, high-fidelity design       | Before/after slider (right side) |

Both are phone screens at 393 × 852. Until the files exist, the slider
falls back to its drawn placeholder automatically, so a missing image
never breaks the page.

More screenshots (flows gallery, hero, research boards) can be added the
same way — put the file here, reference it from the `media` block in
`content/projects/leaf/project.json`.
